<%= form_for @sticker do |f| %>
  <% if @sticker.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@sticker.errors.count, "error") %> prevented
        this sticker from being saved:
      </h2>
      <ul>
        <% @sticker.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= f.label :image %></p>
  <p><%= f.file_field :image %></p>

  <%= f.submit %>

<% end %>
